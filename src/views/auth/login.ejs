<%- include('../includes/header'); %>

    <body>

        <%- include('../includes/menu'); %>

        <main class="login">

            <h1><%= pageTitle %></h1>

            <form class="login__form" method="POST" action="/login">

                <div class="login__field">
                    <label for="emailInput" id="emailLabel" class="<%= emailError.length === 0 ? 'login__label' : 'login__label login__label--error' %>"><%= emailError.length === 0 ? `Email Address*` : emailError %></label>
                    <input
                        type="email"
                        id="emailInput"
                        name="emailInput"
                        class="<%= emailError.length === 0 ? 'login__input' : 'login__input login__input--error' %>"
                        aria-labelledby="emailLabel"
                        placeholder="Please enter your email address"
                        autocomplete="on"
                        required
                    />
                </div>

                <div class="login__field">
                    <label for="passwordInput" id="passwordLabel" class="<%= passwordError.length === 0 ? 'login__label' : 'login__label--error' %>"><%= passwordError.length === 0 ? 'Password*' : 'Error : Password is incorrect' %></label>
                    <input
                        type="password"
                        id="passwordInput"
                        name="passwordInput"
                        class="<%= passwordError === 0 ? 'login__input' : 'login__input login__input--error' %>"
                        aria-labelledby="passwordLabel"                                 
                        placeholder="Please enter your password"
                        autocomplete="on"
                        required
                    />
                </div>

                <input type="hidden" name="csrfToken" value=<%= csrfToken %> />
                <button type="submit" class="button button--yellow">Login</button>

            </form>

            <a href="/new-password" class="login__reset-link">Reset Your Password</a>

        </main>

    </body>
    
<%- include('../includes/footer'); %>